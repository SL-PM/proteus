[package]
name = "proteus-server"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1"
clap = { version = "4", features = ["derive"] }
proteus-core = { path = "../proteus-core" }
quinn = "0.11"
tokio = { version = "1", features = ["full"] }
