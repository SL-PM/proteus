{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core defaults for the main window. Custom app commands (connect/disconnect/get_stats) are not ACL-gated.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
